[project]
name = "today-menu-ai"
version = "0.1.0"
description = "AI menu recommendation service"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "flask-cors",
    "python-dotenv",
    "google-generativeai"
]

[tool.vercel]
entrypoint = "api.recommend:app"